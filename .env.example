TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
ADMIN_CHAT_ID=
FREE_SHIPPING_MIN=100
DEFAULT_SHIPPING=14
DATA_DIR=data
PRODUCTS_CSV=Product_export.csv
B2B_DISCOUNT=15
STRIPE_SECRET_KEY=
STRIPE_CURRENCY=eur
FOLLOWUP_HOURS=24
# URL HTTPS della Mini App. Su Railway lascialo VUOTO: viene ricavato
# automaticamente da RAILWAY_PUBLIC_DOMAIN (lo stesso processo serve la
# Mini App su $PORT). Impostalo solo per usare un hosting esterno.
WEBAPP_URL=
# PORT è iniettata da Railway; in locale impostala per servire la Mini App
# (es. PORT=8080 → http://localhost:8080). Se assente, il bot fa solo polling.
# PORT=8080
